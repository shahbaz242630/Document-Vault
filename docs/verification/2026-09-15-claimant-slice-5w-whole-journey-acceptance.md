# Slice 5W — synthetic whole-journey acceptance verification

Date: 2026-09-15 (Asia/Dubai). Branch: `codex/claimant-whole-journey-acceptance`, based on merged Slice 5U–5V main commit `0a1f29c` plus the preserved 5S–5V session-close documentation commit `55e1f11`.

The new mobile acceptance harness is independently literal-false and disconnected from normal runtime. It joins the actual disabled Slice 5U–5V session journey to the existing shared synthetic submission and claim-state model. The upstream path establishes the injected fresh-AAL2 portal session, freshly asserts it, proves offline-code V2 possession and completes the authenticated handoff. The harness accepts only the exact frozen value-free draft, then advances the established fixed synthetic submission scenario through owner protection, review, encrypted delivery and closure.

The cross-boundary seam deliberately carries no case, tenant, user, portal-session, token, challenge, binding, signature or idempotency identifier. Successful completion exposes only a frozen value-free report: synthetic acceptance, offline-code V2 route profile, closed state, version/event counts, session binding, ledger reconciliation and literal-false identity, relationship, release, decryption and runtime-effect fields. This is local contract-continuity evidence, not proof of hosted or production integration.

Eleven focused 5W tests passed. They cover dormant dependency non-access, the complete route-to-closed path, fresh assertion before route work, exact activation/proof/completion/revoke retry delegation, single signing across ambiguous completion, cancellation and late-result suppression, authority-expanded result rejection, terminal lifecycle closure, authentication drift, immutable value-free results and awaitable disposal. The first focused run exposed child error types escaping on terminal authentication/lifecycle failure; the wrapper now normalizes every operation to the generic 5W unavailable error, and the focused suite passed after repair.

Three new isolation regressions passed. The guard enforces the literal-false approval, exact import allowlist, required value-free controls, no ambient auth/network/browser/native/storage/provider access, no dynamic import and no normal application importer. The existing Slice 5U–5V guard was narrowed to allow this one separately guarded wrapper and continues to reject every other importer. Security CI runs the new guard explicitly.

Final local baseline passed: 726 mobile tests with three established skips, 171 web, 131 shared-types, 42 shared-validation and 359 API tests (1,429 passed total). All 285 serial `scripts/*.test.cjs` tests passed. All workspace typechecks, zero-warning ESLint excluding only generated `supabase/.temp/**` and the two protected local directories, Phase 1 DoD, repository security, GitHub Actions security, mobile-secret scan, production dependency audit, claim vectors/custody, API Vercel bundle and related session/bridge/journey isolation checks passed. The Next 16.3.5 production build generated all 24 pages, and `git diff --check` passed.

The production dependency audit retains only the existing digest-verified `image-size` exception through 2026-09-30. No database acceptance run was required because Slice 5W changes no server, migration, database or hosted boundary.

No hosted MFA/Auth integration, Supabase SDK, normal claimant UI/navigation, persistent session/scenario storage, production native custody/signing, server/database change, provider/notification integration, hosted mutation, deployment, preview promotion, native/EAS build, real claimant data, downstream runtime authority or capability activation occurred.

Delivery: PR #87 targets `main` from `codex/claimant-whole-journey-acceptance`; first published code/evidence head `dcc027f`. The active ten-minute `pr-87-slice-5w-green-red-watcher` owns current exact-head CI, security, native, hosted, Vercel preview/application-smoke and relevant runtime-log evaluation. It stays quiet while pending, repairs and republishes only a confirmed narrow red failure, and reports conclusive green with exact-head evidence. It must not merge, deploy, promote, activate capabilities, use real data, weaken protection, mutate hosted services or start another slice. Local green is not CI or merge evidence.
