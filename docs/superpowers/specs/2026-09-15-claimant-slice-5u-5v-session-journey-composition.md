# Slices 5U–5V — synthetic claimant session journey composition

Selected together as one bounded slice on 2026-09-15 while repaired Slice 5T PR #85 is gated. Add one independently literal-false, runtime-disconnected mobile composition that joins the Slice 5T claimant-portal session client to the Slice 5S session-bound possession/handoff bridge, then governs that chain through one synthetic authenticated-session and lifecycle controller.

The composition accepts only injected synthetic authenticated-session and ordered lifecycle sources, injected portal/possession/handoff transports, an injected proof producer, and an injected synthetic signer. It creates the existing portal client and bridge composition only inside an explicitly approved synthetic test path. It must not import Supabase, normal authentication, navigation, application routes, storage, native modules, ambient network access, or provider adapters.

Portal activation must precede bridge construction. Every start, proof retry, and completion retry requires a fresh server portal-session assertion and the exact unchanged user, authenticated-session, portal-session, and session-version binding. Only the existing exact ambiguous activation, proof, completion, or revoke retry may be delegated; no retry may be regenerated or survive cancellation, backgrounding, authentication drift, session drift, or a terminal lifecycle event.

One monotonically increasing lifecycle source governs both layers. Inactive/background cancels current work, clears retry authority, and suppresses late results. Lock, session end, disable, malformed or regressing lifecycle, invalid authentication, authentication binding drift, portal-session loss, or server session-version drift closes permanently. Explicit revoke clears the portal session and closes the bridge. Disposal is awaitable and suppresses late state commits.

The only successful journey output is the existing frozen, value-free draft summary. Snapshots expose generic progress, portal-active and draft-available booleans, with identity, relationship, intake, review, and release authority fixed false. They expose no bearer, user/session/case/challenge/binding/signature/idempotency material or adapter error detail.

Acceptance: dormant-by-default dependency non-access; strict source and initial-state validation; portal activation before composition; fresh assertion before start and both bridge retries; end-to-end synthetic draft; exact activation and revoke retries; exact proof/completion retry delegation; authentication/session drift closure; foreground cancellation and late-result suppression; terminal/regressing lifecycle closure; assertion failure before possession or signing work; explicit revoke; overlap denial; awaitable disposal; safe frozen snapshots/results; static rejection of activation, ambient auth/network/native/storage/provider adapters and normal runtime importers; focused tests, mobile typecheck, lint, security and existing isolation regressions.

No hosted MFA/Auth integration, Supabase SDK, UI/navigation, persistent session storage, production native custody/signing, server/database change, hosted mutation, deployment, native/EAS build, real claimant data, downstream intake/review/release behavior, notification/provider integration, or capability activation is included.
